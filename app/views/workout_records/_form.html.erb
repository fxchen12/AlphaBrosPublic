<%= form_for(@workout_record) do |f| %>
  <% if @workout_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@workout_record.errors.count, "error") %> prohibited this workout_record from being saved:</h2>

      <ul>
      <% @workout_record.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <center>
    <span>
      <div>
 <div style="float: left;">
       <%= f.text_field :duration, :placeholder => "Time (minutes)" %></div>
 <div style="float: left; "> 
      <%= f.text_field :distance, :placeholder => "Distance (miles)" %></div>

 <div style="float: left;"><%= f.submit "Save", data: { disable_with: 'Saving...' }, :style => "" %></div>
 <br style="clear: left;" />
</div>
   
     

      
    </span>
  </center>
<% end %>