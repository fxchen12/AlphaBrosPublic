<%= form_for(@user) do |f| %>
    <%= f.label :email, "Email" %>
    <%= f.text_field :email %>

    <%= f.label :password, "Password" %>
    <%= f.password_field :password %>
	<br><br>
    <%= f.submit "Login", data: { disable_with: 'Logging In...' } %>
<% end %>